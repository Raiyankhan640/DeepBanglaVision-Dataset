# ── Dataset ──────────────────────────────────────────────────
# Exclude source 103K dataset (huge, cloned separately)
dataset/DeepVision-103K/

# Exclude large parquet files (>100MB, exceeds GitHub limit)
# These raw/ready parquets contain embedded images and are 40-500 MB each
dataset/*/DeepVision_Geometry_*.parquet
dataset/*/DeepBanglaMath_*_Ready_to_Translate.parquet

# Allow small parquet files (<100MB) — translated/final outputs
# e.g. DeepBanglaMath_1K_translated.parquet (~8 MB) ✅

# Allow Excel/CSV chunk files (translation work) ✅
# Allow dataset folder structure ✅

# Secrets
.env

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so

# Virtual environment
venv/
.venv/
env/
ENV/

# Translations working folder
translations/

# Temporary / check scripts
notebooks/_*.py

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
